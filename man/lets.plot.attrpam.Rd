% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_attpam.R
\name{lets.plot.attrpam}
\alias{lets.plot.attrpam}
\title{Plot attrPAM results}
\usage{
lets.plot.attrpam(x, species = NULL, col_rich = NULL, ...)
}
\arguments{
\item{x}{A list returned by \code{\link{lets.attrpam}}.}

\item{species}{Optional. A character string with the name of a species to highlight.
If provided, plots its presence in trait space.}

\item{col_rich}{Optional. A color palette function or vector. If `NULL`, a
default palette is used (red ramp for species maps, red–white ramp for richness).}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Invisibly returns \code{NULL}. Called for side effects (plots).
}
\description{
Plots the richness (or a single species) in the attribute (trait) space
generated by \code{\link{lets.attrpam}}.
}
\details{
Plot an Attribute-space PAM
}
\examples{
\dontrun{
# Example with simulated traits
n <- 2000
Species <- paste0("sp", 1:n)
trait_a <- rnorm(n)
trait_b <- trait_a * 0.2 + rnorm(n)  # correlated trait
df <- data.frame(Species, trait_a, trait_b)

# Build AttrPAM
x <- lets.attrpam(df, n_bins = 30)
lets.plot.attrpam(x)
}
}
