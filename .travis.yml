language: r
sudo: false
cache: packages

r:
  - oldrel
  - release
  - devel

r_github_packages:
  - jimhester/covr

after_success:
  - Rscript -e 'library(covr);coveralls()'
  
addons:
  apt:
    packages:
      - libgdal-dev
      - libproj-dev

